import { useRoutes } from "react-router"

const Rutas = () => {

    const hookRutas = useRoutes(
        [
            {

            }
        ]
    )
  
    return hookRutas
}

export default Rutas